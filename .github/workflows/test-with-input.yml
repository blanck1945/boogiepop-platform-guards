name: Test With Input

on:
  workflow_call:
    inputs:
      owner_username:
        required: true
        type: string

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "input received: ${{ inputs.owner_username }}"
